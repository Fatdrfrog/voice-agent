{
  "name": "@voice-dev-agent/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "if exist dist rmdir /s /q dist"
  },
  "dependencies": {
    "@voice-dev-agent/contracts": "workspace:*",
    "@voice-dev-agent/openclaw-bridge": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
